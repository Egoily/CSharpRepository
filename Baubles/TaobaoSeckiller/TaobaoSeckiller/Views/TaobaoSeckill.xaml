<Page x:Class="TaobaoSeckiller.Views.TaobaoSeckill"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="300"
      d:DesignWidth="300"
      Title="AdvanceSelect">

    <Grid>
        <StackPanel Orientation="Vertical">
            <Button x:Name="BtnLogin"
                    Command="{Binding LoginCommand}">Login</Button>
            <Button x:Name="BtnClearCart"
                    Command="{Binding SettlementCommand}">Clear Cart</Button>
            <Button x:Name="BtnClose"
                    Command="{Binding LogoutCommand}">Close</Button>
        </StackPanel>
    </Grid>
</Page>
